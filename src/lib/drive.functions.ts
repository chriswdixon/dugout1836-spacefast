const NOT_READY = "Google Drive import needs Google credentials connected — use the Google Photos link import instead for now.";
export async function listDriveFolders(_?: unknown): Promise<never> { throw new Error(NOT_READY); }
export async function syncDriveAlbum(_?: unknown): Promise<never> { throw new Error(NOT_READY); }
